{
  "name": "parquetpro", 
  "private": true, 
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "cd backend && .venv/bin/python -m uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && bun run dev",
    "seed": "cd backend && .venv/bin/python scripts/seed_db.py",
    "setup": "cd backend && pip install -r requirements.txt && cd ../frontend && bun install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
